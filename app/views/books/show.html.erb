  <%= flash[:notice] %>
  <%= flash[:alert] %>
<div>
<h2>User info</h2>
  <%= image_tag @user.get_image(100,100) %>
</div>
  <table>
  <tbody>
  <tr>
  <th>name</th>
  <th><%= @user.name %></th>
  </tr>
  <tr>
  <th>introduction</th>
  <th><%= @user.introduction %></th>
  </tr>
  <% if @user.id == current_user.id %>
  <% end %>
  </tbody>
  </table>
  <p><%= link_to "編集",edit_user_path(@user) %></p>
<h2>New book</h2>
  <%= form_with model: @book, url:'/books',method: :post do |f| %>
<div class="field">
  <%= f.label :Title%><br>
  <%= f.text_field :title%>
</div>
<div class="field">
  <%= f.label :Opinion%><br>
  <%= f.text_area :body%><br>
  <%= f.submit'Create Book' %>
</div>
  <% end %>
  <h2>Book detail</h2>
<table>
  <thead>
    <tr>
    <th>Title</th>
    <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <tr>
    <td><%= image_tag @user.get_image(100,100) %></td>
    <td><a href="/users/:id"><%= @user.name %></a></td>
    <td><a href="/books/:id"><%= book.title %></a></td>
    <td><%= @book.body %></td>

    <td><%= link_to "Edit",edit_user_path(@user) %></td>
    <td><%= link_to "destroy", destroy_user_path(@user.id), method: :delete, data:{confirm:"Are you sure?" }%></td>
    </tr>
  </tbody>
 </table>